@{
    ViewData["Title"] = "Ana Sayfa";
}

<style>
    /* Hero bölümü */
    .hero {
        height: calc(100vh - 56px); /* navbar yüksekliğini çıkar */
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        background-color: #333; /* koyu gri ton */
        color: #fff;
        text-align: center;
        padding: 0 20px;
    }

        .hero h1 {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .hero p {
            font-size: 1.25rem;
            margin-bottom: 2rem;
            max-width: 600px;
        }

        .hero .btn-cta {
            font-size: 1.125rem;
            padding: 0.75rem 1.75rem;
            margin: 0 0.5rem;
        }
</style>

<div class="hero">
    <h1>Web IoT Platformuna Hoş Geldiniz</h1>
    <p>Gerçek zamanlı veri takibi, cihaz yönetimi ve analitikler — hepsi tek bir merkezden.</p>
    <div>
        <a asp-area="Identity" asp-page="/Account/Login" class="btn btn-primary btn-cta">Giriş Yap / Kayıt Ol</a>
        <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-outline-light btn-cta">Ücretsiz Başlayın</a>
    </div>
</div>
